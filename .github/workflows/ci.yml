name: CI

on: repository_dispatch

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run a one-line script
      run: |
        echo DIFFERENT BRANCH
        echo Hello from $GITHUB_REF
